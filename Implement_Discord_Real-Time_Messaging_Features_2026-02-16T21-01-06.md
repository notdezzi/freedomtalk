[ ] NAME:Current Task List DESCRIPTION:Root task for conversation __NEW_AGENT__
-[x] NAME:1.1. Install Required npm Packages DESCRIPTION:Install marked@^12.0.0, highlight.js@^11.9.0, open-graph-scraper@^6.5.0, minio@^8.0.0, and @types/marked to packages/api. Navigate to packages/api, run npm install commands, verify in package.json, run npm install at workspace root. ACCEPTANCE: All 5 packages in package.json, no critical npm audit issues, npm run build succeeds. VERIFY: Check package.json, run npm audit, run npm run build.
-[x] NAME:1.2. Extend Shared Constants with New Validation Rules DESCRIPTION:Add REACTION (MAX_PER_MESSAGE: 20, MAX_EMOJI_NAME_LENGTH: 32), EMBED (MAX_PER_MESSAGE: 10, MAX_TITLE_LENGTH: 256, MAX_DESCRIPTION_LENGTH: 4096, MAX_FIELDS: 25, MAX_FIELD_NAME_LENGTH: 256, MAX_FIELD_VALUE_LENGTH: 1024, MAX_FOOTER_LENGTH: 2048, MAX_AUTHOR_NAME_LENGTH: 256, MAX_TOTAL_CHARACTERS: 6000), ATTACHMENT (MAX_PER_MESSAGE: 10, MAX_FILE_SIZE: 26214400, ALLOWED_IMAGE_TYPES, ALLOWED_VIDEO_TYPES, ALLOWED_AUDIO_TYPES, ALLOWED_DOCUMENT_TYPES), DM_CHANNEL (MIN_PARTICIPANTS: 2, MAX_PARTICIPANTS: 10, MAX_NAME_LENGTH: 100) to packages/shared/src/constants/index.ts. Export all, run npm run build in packages/shared. ACCEPTANCE: All constants typed and exported, TypeScript compiles, accessible from packages/api. VERIFY: Import in test file, run npm run build, check .d.ts files.
-[x] NAME:1.3. Extend WebSocket Events Constants DESCRIPTION:Add REACTION_ADD: 'reaction:add', REACTION_REMOVE: 'reaction:remove', REACTION_REMOVE_ALL: 'reaction:remove_all', REACTION_REMOVE_EMOJI: 'reaction:remove_emoji', DM_CHANNEL_CREATE: 'dm_channel:create', DM_CHANNEL_UPDATE: 'dm_channel:update', DM_CHANNEL_DELETE: 'dm_channel:delete', DM_CHANNEL_RECIPIENT_ADD: 'dm_channel:recipient_add', DM_CHANNEL_RECIPIENT_REMOVE: 'dm_channel:recipient_remove' to WS_EVENTS in packages/shared/src/constants/index.ts. Verify WebSocketEvent type includes new events. Run npm run build. ACCEPTANCE: All events typed, WebSocketEvent type updated, no TypeScript errors. VERIFY: Check .d.ts file, import WS_EVENTS in test.
-[x] NAME:1.4. Create MinIO Storage Service DESCRIPTION:Create packages/api/src/services/storage/minio.service.ts with MinIO client (env: MINIO_ENDPOINT, MINIO_PORT, MINIO_ACCESS_KEY, MINIO_SECRET_KEY, MINIO_USE_SSL). Implement ensureBucket(bucketName), uploadFile(bucketName, objectName, stream, size, metadata), downloadFile(bucketName, objectName), deleteFile(bucketName, objectName), getFileUrl(bucketName, objectName, expiry). Add StorageError class, Pino logging, export singleton. ACCEPTANCE: Connects to MinIO, all CRUD operations work, errors logged, TypeScript strict mode. VERIFY: Unit tests (mocked MinIO), integration test with real MinIO, test upload/download/delete cycle.
-[x] NAME:2.1. Create Reactions Table Migration DESCRIPTION:Create migration packages/api/migrations/YYYYMMDDHHMMSS_create_reactions_table.ts. Schema: id (VARCHAR(20) PK), message_id (VARCHAR(20) FK→messages.id CASCADE), user_id (VARCHAR(20) FK→users.id CASCADE), emoji_type (VARCHAR(10) CHECK IN ('unicode','custom')), emoji_id (VARCHAR(20) nullable), emoji_unicode (VARCHAR(20) nullable), created_at (TIMESTAMPTZ NOT NULL DEFAULT NOW()). Indexes: message_id, user_id, (message_id, user_id, emoji_type, COALESCE(emoji_id, emoji_unicode)) UNIQUE. Check constraints: emoji_type IN ('unicode','custom'), XOR constraint for emoji_id/emoji_unicode. ACCEPTANCE: Migration runs, table exists, constraints enforced, rollback works. VERIFY: Run migration, test constraint violations, test unique constraint, run rollback.
-[x] NAME:2.2. Create Message Embeds Table Migration DESCRIPTION:Create migration for message_embeds table. Schema: id (VARCHAR(20) PK), message_id (VARCHAR(20) FK→messages.id CASCADE), type (VARCHAR(20) DEFAULT 'rich'), title (VARCHAR(256) nullable), description (TEXT nullable), url (VARCHAR(2048) nullable), timestamp (TIMESTAMPTZ nullable), color (INTEGER nullable), footer_text (VARCHAR(2048) nullable), footer_icon_url (VARCHAR(500) nullable), image_url (VARCHAR(500) nullable), thumbnail_url (VARCHAR(500) nullable), author_name (VARCHAR(256) nullable), author_url (VARCHAR(500) nullable), author_icon_url (VARCHAR(500) nullable), fields (JSONB nullable), created_at (TIMESTAMPTZ NOT NULL DEFAULT NOW()). Indexes: message_id, type. Check constraints: type IN ('rich','image','video','link','article'), title length ≤256, footer length ≤2048, author_name length ≤256. ACCEPTANCE: Migration runs, JSONB accepts arrays, constraints work, cascade deletes. VERIFY: Insert test embed with all fields, test JSONB array, test constraints, delete parent message.
-[x] NAME:2.3. Create Message Attachments Table Migration DESCRIPTION:Create migration for message_attachments table. Schema: id (VARCHAR(20) PK), message_id (VARCHAR(20) FK→messages.id CASCADE), filename (VARCHAR(255) NOT NULL), size (BIGINT NOT NULL), mime_type (VARCHAR(100) NOT NULL), object_path (VARCHAR(500) NOT NULL), width (INTEGER nullable), height (INTEGER nullable), thumbnail_path (VARCHAR(500) nullable), uploaded_by (VARCHAR(20) FK→users.id SET NULL), created_at (TIMESTAMPTZ NOT NULL DEFAULT NOW()). Indexes: message_id, uploaded_by, mime_type. Check constraints: size > 0, (width IS NULL OR width > 0) AND (height IS NULL OR height > 0). ACCEPTANCE: Migration runs, constraints enforced, foreign keys work, indexes improve performance. VERIFY: Insert test attachment, test constraints (negative size), test cascade delete, query by message_id with EXPLAIN.
-[x] NAME:2.4. Create Custom Emojis Table Migration DESCRIPTION:Create migration for custom_emojis table. Schema: id (VARCHAR(20) PK), server_id (VARCHAR(20) FK→servers.id CASCADE), name (VARCHAR(32) NOT NULL), image_url (VARCHAR(500) NOT NULL), animated (BOOLEAN DEFAULT FALSE), created_by (VARCHAR(20) FK→users.id SET NULL), created_at (TIMESTAMPTZ NOT NULL DEFAULT NOW()), updated_at (TIMESTAMPTZ NOT NULL DEFAULT NOW()). Indexes: server_id, name, (server_id, name) UNIQUE. Check constraints: name ~ '^[a-zA-Z0-9_]+$', char_length(name) ≥2 AND ≤32. ACCEPTANCE: Migration runs, unique constraint prevents duplicate names per server, regex enforces valid names, foreign keys work. VERIFY: Insert test emoji, test unique constraint, test regex constraint (invalid chars), delete server and verify cascade.
-[x] NAME:2.5. Create DM Channels Table Migration DESCRIPTION:Create migration for dm_channels table. Schema: id (VARCHAR(20) PK), type (VARCHAR(10) NOT NULL CHECK IN ('dm','group_dm')), name (VARCHAR(100) nullable), icon_url (VARCHAR(500) nullable), owner_id (VARCHAR(20) FK→users.id SET NULL), created_at (TIMESTAMPTZ NOT NULL DEFAULT NOW()), updated_at (TIMESTAMPTZ NOT NULL DEFAULT NOW()). Indexes: type, owner_id, created_at. Check constraints: type IN ('dm','group_dm'), (type='dm' AND name IS NULL) OR (type='group_dm'), name IS NULL OR (char_length(name) ≥1 AND ≤100). ACCEPTANCE: Migration runs, type constraint enforced, check constraints prevent invalid combinations, indexes improve performance. VERIFY: Insert DM (type='dm', name=NULL), insert group DM (type='group_dm', name='Test'), test constraint violations, verify indexes with EXPLAIN.
-[x] NAME:2.6. Create DM Channel Participants Table Migration DESCRIPTION:Create migration for dm_channel_participants table. Schema: id (VARCHAR(20) PK), dm_channel_id (VARCHAR(20) FK→dm_channels.id CASCADE), user_id (VARCHAR(20) FK→users.id CASCADE), joined_at (TIMESTAMPTZ NOT NULL DEFAULT NOW()), left_at (TIMESTAMPTZ nullable), is_active (BOOLEAN DEFAULT TRUE). Indexes: dm_channel_id, user_id, (dm_channel_id, user_id), is_active. Unique partial index: CREATE UNIQUE INDEX idx_dm_participants_active_unique ON dm_channel_participants (dm_channel_id, user_id) WHERE is_active = true. ACCEPTANCE: Migration runs, unique constraint prevents duplicate active participants, foreign keys cascade, indexes improve joins. VERIFY: Insert participants, test unique constraint (duplicate active), test cascade delete (delete DM channel), query all DMs for user with EXPLAIN.
-[x] NAME:3.1. Create Reaction Service DESCRIPTION:Create packages/api/src/services/reaction/reaction.service.ts with addReaction(messageId, userId, emojiType, emojiId, emojiUnicode) - validate message exists, validate emoji, check 20-reaction limit, insert, return reaction; removeReaction(messageId, userId, emojiType, emojiId, emojiUnicode) - verify ownership, delete; removeAllReactions(messageId, requesterId) - verify permission (author/admin), delete all; removeReactionsByEmoji(messageId, emojiType, emojiId, emojiUnicode, requesterId) - verify permission, delete matching; getReactionsByMessage(messageId) - get all, group by emoji, return counts and user lists; getReactionUsers(messageId, emojiType, emojiId, emojiUnicode, limit, offset) - paginated users. Error handling: message not found, limit exceeded, invalid emoji, duplicate. Pino logging. ACCEPTANCE: All methods work, validation prevents invalid states, errors logged, TypeScript strict mode. VERIFY: Unit tests for each method, test limit enforcement, test duplicate prevention, test permissions.
-[x] NAME:3.2. Create Reaction API Endpoints DESCRIPTION:Create packages/api/src/routes/reactions.routes.ts with PUT /api/v1/messages/:messageId/reactions/:emoji (add reaction), DELETE /api/v1/messages/:messageId/reactions/:emoji/@me (remove own), DELETE /api/v1/messages/:messageId/reactions/:emoji (remove all of emoji, requires permission), DELETE /api/v1/messages/:messageId/reactions (remove all, requires permission), GET /api/v1/messages/:messageId/reactions/:emoji (get users, paginated), GET /api/v1/messages/:messageId/reactions (get all). Zod validation for emoji parameter and pagination. requireAuth middleware. Rate limiting (10 reactions per 10 seconds). Swagger docs. Register in packages/api/src/routes/index.ts. ACCEPTANCE: All endpoints return correct status codes, validation rejects invalid input, auth required, Swagger docs generated. VERIFY: Integration tests for each endpoint, test auth requirement, test rate limiting, verify Swagger UI.
-[x] NAME:3.3. Create Reaction WebSocket Events DESCRIPTION:Create packages/api/src/services/websocket/handlers/reaction.handler.ts with reaction:add (validate, call reactionService.addReaction(), broadcast to channel), reaction:remove (validate, call removeReaction(), broadcast), reaction:remove_all (validate permission, call removeAllReactions(), broadcast), reaction:remove_emoji (validate permission, call removeReactionsByEmoji(), broadcast). Zod validation, auth check (socket.data.userId exists), error handling (emit error events), broadcast to appropriate room (channel or DM). Register in packages/api/src/services/websocket/handlers/index.ts. ACCEPTANCE: Events broadcast to correct rooms, validation prevents invalid operations, errors emitted to client, all users in room receive updates. VERIFY: Integration tests with multiple connected clients, test room isolation, test error handling, load test with concurrent reactions.
-[x] NAME:4.1. Create Embed Service DESCRIPTION:Create packages/api/src/services/embed/embed.service.ts with createEmbed(messageId, embedData) - validate limits (title 256, description 4096, fields 25, etc.), insert, return embed; createEmbeds(messageId, embeds) - validate total chars ≤6000, validate max 10 embeds, insert all in transaction; getEmbedsByMessage(messageId) - retrieve all, parse JSONB; updateEmbed(embedId, embedData) - update, re-validate; deleteEmbed(embedId); validateEmbedData(embedData) - validate all limits, return errors; calculateTotalCharacters(embeds) - calculate total for 6000 limit. TypeScript interfaces: EmbedData, EmbedField. Error handling for validation failures. Pino logging. ACCEPTANCE: All validation limits enforced, JSONB properly serialized/deserialized, errors provide clear messages, TypeScript strict mode. VERIFY: Unit tests for each method, test all validation limits, test JSONB field array handling, test transaction rollback on validation failure.
-[x] NAME:4.2. Create Link Preview Service DESCRIPTION:Create packages/api/src/services/embed/link-preview.service.ts with generatePreview(url) - fetch URL, extract Open Graph metadata, return embed data; extractUrls(content) - parse message for URLs using regex; shouldGeneratePreview(url) - check whitelist/blacklist; cachePreview(url, embedData) - cache in Redis (TTL 24h); getCachedPreview(url) - retrieve cached. Configure open-graph-scraper: timeout 5s, follow redirects (max 3), custom user agent. Map OG properties: og:title→title, og:description→description, og:image→image_url, og:url→url, og:site_name→author_name, article:published_time→timestamp. Error handling: network timeouts, invalid URLs, missing OG tags, rate limiting. Pino logging. ACCEPTANCE: Successfully extracts OG metadata, handles missing tags gracefully, caching reduces external requests, timeouts prevent hanging. VERIFY: Unit tests with mocked HTTP, integration tests with real URLs (YouTube, Twitter, GitHub), test timeout handling, test caching (verify Redis keys).
-[x] NAME:4.3. Integrate Embeds into Message Creation DESCRIPTION:Modify packages/api/src/services/message/message.service.ts createMessage() to add optional embeds parameter, after message insertion call embedService.createEmbeds(messageId, embeds) if provided, if message contains URLs and no embeds call linkPreviewService.generatePreview() asynchronously for each URL, wrap message + embed creation in transaction. Update return type to include embeds. Add validation for max 10 embeds. Update Zod schema for message creation to include optional embeds array. Update WebSocket broadcast to include embeds in message payload. ACCEPTANCE: Messages created with embeds, link previews generated automatically, transaction ensures atomicity, WebSocket events include embeds. VERIFY: Integration test create message with embeds, integration test create message with URL verify preview generated, test transaction rollback (force embed validation failure), verify WebSocket payload includes embeds.
-[x] NAME:5.1. Create Attachment Upload Service DESCRIPTION:Create packages/api/src/services/attachment/attachment.service.ts with uploadAttachment(file, userId, messageId) - validate file (size, MIME type), generate unique object path, upload to MinIO, generate thumbnail if image, insert metadata, return attachment; uploadAttachments(files, userId, messageId) - validate max 10, process in parallel, wrap in transaction; getAttachmentsByMessage(messageId); deleteAttachment(attachmentId, userId) - verify ownership, delete from MinIO, delete from database; generateThumbnail(file, objectPath) - use Sharp to generate thumbnail (max 300x300), upload to MinIO, return thumbnail path; validateFile(file) - check size ≤25MB, check MIME type in allowed list, return errors. TypeScript interfaces: AttachmentData, UploadResult. Error handling: file too large, invalid MIME type, MinIO upload failures, thumbnail generation failures. Pino logging. ACCEPTANCE: Files uploaded to MinIO, metadata stored in database, thumbnails generated for images, validation prevents invalid uploads. VERIFY: Unit tests for validation, integration test upload image verify thumbnail, integration test upload video verify no thumbnail, test file size limit, test MIME type validation.
-[x] NAME:5.2. Create Attachment Upload API Endpoint DESCRIPTION:Create packages/api/src/routes/attachments.routes.ts with POST /api/v1/messages/:messageId/attachments. Configure Multer middleware: memory storage (for streaming to MinIO), limits 25MB per file max 10 files, file filter check MIME type. Use Multer to parse multipart/form-data, call attachmentService.uploadAttachments(files, userId, messageId), return array of attachment objects with URLs. requireAuth middleware. Rate limiting (5 uploads per minute per user). Swagger docs. Register in packages/api/src/routes/index.ts. ACCEPTANCE: Endpoint accepts multipart/form-data, multiple files uploaded successfully, validation rejects invalid files, rate limiting prevents abuse. VERIFY: Integration test upload single file, integration test upload multiple files (10), test file size limit (upload 26MB expect 413), test MIME type validation (upload .exe expect 400), test rate limiting.
-[x] NAME:5.3. Integrate Attachments into Message Creation DESCRIPTION:Modify packages/api/src/routes/messages.routes.ts POST /api/v1/channels/:channelId/messages to add Multer middleware to parse multipart/form-data, accept content field (text) and files field (attachments), parse embeds field from JSON string if provided, create message first then upload attachments with message ID, wrap in transaction. Update Zod schema to handle multipart data. Update response to include attachments. Update WebSocket broadcast to include attachments. ACCEPTANCE: Single request creates message with text, embeds, and attachments, transaction ensures atomicity, WebSocket broadcast includes all data. VERIFY: Integration test create message with all fields, test transaction rollback (force validation failure after file upload), verify WebSocket payload includes attachments.
-[x] NAME:6.1. Create Markdown Parsing Service DESCRIPTION:Create packages/api/src/services/formatting/markdown.service.ts with parseMarkdown(content) - parse markdown to HTML using marked, apply custom renderer for Discord-specific syntax; parseToAST(content) - parse to AST for analysis; stripMarkdown(content) - remove markdown formatting. Configure marked: GFM true, breaks true, sanitize false. Create custom renderer extending marked.Renderer: override code() to add syntax highlighting, override link() to add rel='noopener noreferrer', override image() to add lazy loading, add custom token for spoilers (||text||), add custom token for mentions (<@userId>, <@&roleId>, <#channelId>). Error handling for malformed markdown. Pino logging. ACCEPTANCE: Markdown parsed correctly to HTML, custom syntax (spoilers, mentions) rendered, no XSS vulnerabilities, performance acceptable for 2000 char messages. VERIFY: Unit tests for all markdown features (bold, italic, code, links, etc.), test custom syntax (spoilers, mentions), security test attempt XSS injection, performance test parse 2000 char message.
-[x] NAME:6.2. Create Syntax Highlighting Service DESCRIPTION:Create packages/api/src/services/formatting/syntax-highlight.service.ts with highlightCode(code, language?) - highlight code, auto-detect language if not specified, return HTML with classes; getSupportedLanguages() - return list; isLanguageSupported(language) - check if supported. Configure highlight.js: load common languages (javascript, typescript, python, java, c, cpp, rust, go, etc.), enable auto-detection with max 5 language attempts. Add CSS class prefix: hljs-. Error handling for invalid code. Pino logging. ACCEPTANCE: Code highlighted correctly, auto-detection works for common languages, explicit language specification works, no errors on invalid code. VERIFY: Unit tests for each supported language, test auto-detection (provide code without language), test invalid language (expect graceful fallback), performance test highlight 1000-line code block.
-[x] NAME:6.3. Create Mention Parsing Service DESCRIPTION:Create packages/api/src/services/formatting/mention.service.ts with parseMentions(content) - extract all mentions using regex, return array of {type, id, raw}; validateMentions(mentions, channelId) - verify mentioned users/roles/channels exist and accessible in channel context; getMentionedUsers(content, channelId) - extract and validate user mentions, return user IDs for notifications; replaceMentionsWithNames(content) - replace mention IDs with display names for notifications; suppressMentions(content) - escape mentions. Define regex patterns: User <@(\d{20})>, Role <@&(\d{20})>, Channel <#(\d{20})>, Everyone @everyone, Here @here. Add database queries to verify: user exists and is member of channel's server, role exists in channel's server, channel exists in same server. Error handling for invalid mentions. Pino logging. ACCEPTANCE: All mention types parsed correctly, invalid mentions detected, mention validation prevents non-existent users/roles/channels, mention suppression works. VERIFY: Unit tests for regex patterns, test each mention type, test invalid mentions (non-existent IDs), test mention suppression (in code blocks, backticks).
-[x] NAME:6.4. Integrate Formatting into Message Service DESCRIPTION:Modify packages/api/src/services/message/message.service.ts createMessage() to call mentionService.parseMentions(content) to extract mentions, call mentionService.validateMentions(mentions, channelId) to verify mentions, store mention count in message metadata (for notification purposes), call markdownService.parseMarkdown(content) to validate markdown (optional, for preview generation). Modify updateMessage() to re-parse mentions and markdown, update mention count. Add parsed_content field to message response (optional, for client-side rendering). Update WebSocket broadcast to include mention data. ACCEPTANCE: Mentions parsed and validated at creation, invalid mentions rejected or flagged, mention count tracked, no performance degradation. VERIFY: Integration test create message with mentions, test invalid mention rejection, performance test create 100 messages with mentions, verify mention count in database.
-[x] NAME:7.1. Create DM Channel Service DESCRIPTION:Create packages/api/src/services/dm/dm-channel.service.ts with createDM(userId1, userId2) - check if DM already exists between users, if not create new DM channel, add both users as participants, return DM channel; createGroupDM(ownerId, participantIds, name?, iconUrl?) - validate 3-10 participants, create group DM channel, add all participants, return group DM; getDMByParticipants(userId1, userId2) - find existing DM between two users; getDMsByUser(userId, limit, offset) - get all DMs for user (paginated), include last message preview; addParticipant(dmChannelId, userId, requesterId) - verify group DM, verify requester is owner or participant, verify max 10 participants, add participant, broadcast event; removeParticipant(dmChannelId, userId, requesterId) - verify permission (owner or self), mark participant as inactive (left_at), broadcast event; updateGroupDM(dmChannelId, updates, requesterId) - verify group DM, verify requester is owner, update channel, broadcast event; deleteDM(dmChannelId, userId) - soft delete for user (mark participant as inactive). Validation: participant count (2-10), group DM name length (1-100), owner permissions. Error handling: duplicate DM creation, invalid participant IDs, permission violations. Pino logging. ACCEPTANCE: DMs created without duplicates, group DMs support 3-10 participants, participant management works correctly, permissions enforced. VERIFY: Unit tests for each method, test duplicate DM prevention (concurrent creation), test participant limits (min 2, max 10), test permission checks (non-owner can't add participants).
-[x] NAME:7.2. Create DM Channel API Endpoints DESCRIPTION:Create packages/api/src/routes/dm.routes.ts with POST /api/v1/users/@me/channels (create DM or group DM, body: {recipient_id} or {recipients: [], name?, icon_url?}), GET /api/v1/users/@me/channels (get all DMs for current user, paginated), GET /api/v1/channels/:channelId (get DM channel details), PATCH /api/v1/channels/:channelId (update group DM name, icon), DELETE /api/v1/channels/:channelId (leave DM/group DM), PUT /api/v1/channels/:channelId/recipients/:userId (add participant to group DM), DELETE /api/v1/channels/:channelId/recipients/:userId (remove participant from group DM). Zod validation schemas for: create DM request (recipient_id or recipients array), update group DM request (name, icon_url), pagination params. requireAuth middleware to all endpoints. Rate limiting (10 DM creations per hour per user). Swagger docs. Register in packages/api/src/routes/index.ts. ACCEPTANCE: All endpoints work correctly, validation prevents invalid operations, authentication and authorization enforced, rate limiting prevents abuse. VERIFY: Integration tests for each endpoint, test authorization (user can't access DM they're not in), test rate limiting, verify Swagger docs.
-[x] NAME:7.3. Update WebSocket Message Router for DM Routing DESCRIPTION:Implement DM routing in packages/api/src/services/websocket/message.router.ts by completing routeDM() method. Extract DM channel ID from message, query dm_channel_participants to get all active participants, for each participant get their active socket connections, broadcast message to all participant sockets, use Redis pub/sub for multi-server broadcasting. Error handling for: DM channel not found, no active participants, broadcast failures. Add logging for DM message routing. Update room manager to support DM rooms (format: dm:${channelId}). ACCEPTANCE: DM messages broadcast to all participants, no messages leak to non-participants, multi-server broadcasting works via Redis, performance acceptable for 10-participant group DMs. VERIFY: Integration test send DM verify both users receive it, integration test send group DM verify all participants receive it, test privacy (user not in DM doesn't receive message), load test 10-participant group DM with high message volume.
-[x] NAME:7.4. Create DM Channel WebSocket Events DESCRIPTION:Create packages/api/src/services/websocket/handlers/dm.handler.ts with dm_channel:create (validate input, call dmChannelService.createDM() or createGroupDM(), broadcast to all participants), dm_channel:update (validate permission, call dmChannelService.updateGroupDM(), broadcast to participants), dm_channel:delete (call dmChannelService.deleteDM(), broadcast to participants), dm_channel:recipient_add (validate permission, call dmChannelService.addParticipant(), broadcast to all participants including new one), dm_channel:recipient_remove (validate permission, call dmChannelService.removeParticipant(), broadcast to all participants). Zod validation, auth check, error handling (emit error events), broadcast events to DM room and individual user rooms. Register in packages/api/src/services/websocket/handlers/index.ts. ACCEPTANCE: Events broadcast to correct users, validation prevents invalid operations, permissions enforced, all participants receive updates. VERIFY: Integration tests with multiple connected clients, test permission checks (non-owner can't add participants), test event broadcasting (all participants receive events), test privacy (non-participants don't receive events).
-[x] NAME:8.1. Extend Message Service for DM Messages DESCRIPTION:Modify packages/api/src/services/message/message.service.ts createMessage() to accept optional dmChannelId parameter (mutually exclusive with channelId), if dmChannelId provided verify requester is participant in DM, store channel_id as NULL for DM messages (or create separate field), add DM channel reference to message metadata. Modify getMessages() to support querying by dmChannelId, verify requester is participant before returning messages. Modify updateMessage() and deleteMessage() to support DM messages, verify requester is author or has permission. Update WebSocket routing to use routeDM() for DM messages. ACCEPTANCE: DM messages created successfully, participant verification works, DM messages routed correctly via WebSocket, privacy enforced. VERIFY: Integration test create DM message, test participant verification (non-participant can't access), test WebSocket routing (message delivered to participants), test message history retrieval.
-[x] NAME:8.2. Create DM Message API Endpoints DESCRIPTION:Open packages/api/src/routes/dm.routes.ts and add POST /api/v1/channels/:channelId/messages (create DM message, reuse existing endpoint add DM channel support), GET /api/v1/channels/:channelId/messages (get DM message history, paginated), GET /api/v1/channels/:channelId/messages/:messageId (get specific DM message), PATCH /api/v1/channels/:channelId/messages/:messageId (edit DM message), DELETE /api/v1/channels/:channelId/messages/:messageId (delete DM message). Add middleware to verify: channel is a DM channel, requester is participant. Zod validation schemas. Rate limiting (5 messages per 5 seconds per DM). Swagger documentation. ACCEPTANCE: All endpoints work correctly, participant verification enforced, rate limiting prevents spam, pagination works for message history. VERIFY: Integration tests for each endpoint, test participant verification, test rate limiting, test pagination (create 100 messages, paginate through them).
-[x] NAME:8.3. Implement DM Presence Indicators DESCRIPTION:Extend packages/api/src/services/websocket/presence.manager.ts to add subscribeToDMPresence(userId, dmChannelId) - subscribe to presence updates for DM participants; unsubscribeFromDMPresence(userId, dmChannelId) - unsubscribe; getDMParticipantPresence(dmChannelId) - get online status of all DM participants. Modify updatePresence() to broadcast presence updates to all DMs user is in, use DM room format: dm:${channelId}. Modify typing indicators: support DM channels in startTyping() and stopTyping(), broadcast typing events to DM participants. Add caching for DM participant lists (Redis, TTL 5 minutes). ACCEPTANCE: Presence updates broadcast to DM participants, typing indicators work in DMs, no privacy leaks, performance acceptable. VERIFY: Integration test user goes online DM participants receive presence update, integration test user types in DM participants see typing indicator, test privacy (non-participant doesn't receive presence), load test user in 50 DMs verify presence broadcast performance.
-[x] NAME:8.4. Implement DM Notification Preferences DESCRIPTION:Create migration for dm_notification_settings table. Schema: id (VARCHAR(20) PK), user_id (VARCHAR(20) FK→users.id CASCADE), dm_channel_id (VARCHAR(20) FK→dm_channels.id CASCADE), is_muted (BOOLEAN DEFAULT FALSE), mute_until (TIMESTAMPTZ nullable), notification_level (VARCHAR(20) DEFAULT 'all' CHECK IN ('all','mentions','none')), created_at (TIMESTAMPTZ NOT NULL DEFAULT NOW()), updated_at (TIMESTAMPTZ NOT NULL DEFAULT NOW()). Indexes: (user_id, dm_channel_id) UNIQUE. Create dmNotificationService with updateSettings(userId, dmChannelId, settings), getSettings(userId, dmChannelId), checkShouldNotify(userId, dmChannelId, message). Add API endpoints: PUT /api/v1/channels/:channelId/notification-settings, GET /api/v1/channels/:channelId/notification-settings. Integrate with message creation to check settings before sending notifications. Cache settings in Redis (TTL 10 minutes). ACCEPTANCE: Settings stored and retrieved correctly, muted DMs don't send notifications, notification levels respected, temporary mutes expire correctly, caching improves performance. VERIFY: Integration test mute DM verify no notifications, test notification levels (all/mentions/none), test temporary mute expiration, test caching (verify Redis keys).
-[ ] NAME:9.1. Create Integration Tests for Reactions DESCRIPTION:Create packages/api/src/services/reaction/__tests__/integration/reaction.integration.test.ts. Test scenarios: add unicode reaction to message, add custom emoji reaction, remove own reaction, remove all reactions (as author), remove reactions by emoji (as admin), get reactions by message, get reaction users (paginated), test 20-reaction limit enforcement, test duplicate reaction prevention, test reaction on non-existent message, test WebSocket broadcast (verify all channel members receive reaction events), test concurrent reactions (race conditions). Use real database and Redis. Setup: create test server, channels, users, messages. Teardown: clean database. ACCEPTANCE: All test scenarios pass, 80% code coverage for reaction service and routes, tests run in <5 seconds, no flaky tests. VERIFY: Run npm test, check coverage report, run tests 10 times verify no flakes.
-[ ] NAME:9.2. Create Integration Tests for Embeds and Attachments DESCRIPTION:Create packages/api/src/services/embed/__tests__/integration/embed.integration.test.ts and packages/api/src/services/attachment/__tests__/integration/attachment.integration.test.ts. Embed tests: create message with embeds, test embed validation limits (title 256, description 4096, total 6000 chars), test max 10 embeds, test link preview generation (mock HTTP), test link preview caching, test embed cascade delete. Attachment tests: upload single file, upload multiple files (10), test file size limit (25MB), test MIME type validation, test thumbnail generation for images, test attachment cascade delete, test MinIO integration (upload/download/delete), test concurrent uploads. Use real database, Redis, and MinIO. ACCEPTANCE: All test scenarios pass, 80% code coverage, tests run in <10 seconds, MinIO cleanup after tests. VERIFY: Run npm test, check coverage, verify MinIO bucket empty after tests.
-[ ] NAME:9.3. Create Integration Tests for DM Channels DESCRIPTION:Create packages/api/src/services/dm/__tests__/integration/dm.integration.test.ts. Test scenarios: create DM between two users, test duplicate DM prevention, create group DM (3-10 participants), test participant limits (min 2, max 10), add participant to group DM, remove participant from group DM, test participant permissions (owner vs member), update group DM (name, icon), delete DM (soft delete), get DMs for user (paginated), send DM message, test DM message routing (WebSocket), test DM presence indicators, test DM notification settings, test privacy (non-participant can't access DM), test concurrent DM creation (race conditions). Use real database, Redis, and WebSocket connections. ACCEPTANCE: All test scenarios pass, 80% code coverage for DM service and routes, tests run in <10 seconds, no privacy leaks. VERIFY: Run npm test, check coverage, test privacy scenarios thoroughly.
-[ ] NAME:9.4. Create Load Tests for Real-Time Messaging DESCRIPTION:Create packages/api/src/__tests__/load/messaging.load.test.ts using artillery or k6. Test scenarios: 100 concurrent users sending messages (10 msg/sec each), 1000 concurrent WebSocket connections, 50 users reacting to same message simultaneously, 100 users uploading attachments concurrently, 10-participant group DM with high message volume (100 msg/sec), presence updates for 1000 online users, typing indicators for 100 concurrent typers. Metrics to measure: message latency (p50, p95, p99), WebSocket connection time, reaction add latency, attachment upload time, memory usage, CPU usage, Redis operations/sec, database queries/sec. Acceptance criteria: p95 message latency <100ms, p99 <500ms, WebSocket connection <1s, no memory leaks, no dropped messages. Generate load test report. ACCEPTANCE: All scenarios complete without errors, latency targets met, no resource exhaustion, report generated. VERIFY: Run load tests, analyze report, check for memory leaks (heap snapshots), verify no dropped messages.
-[ ] NAME:9.5. Create End-to-End Tests DESCRIPTION:Create packages/api/src/__tests__/e2e/messaging.e2e.test.ts. Test complete user flows: User A sends message with text, embeds, and attachments to channel, User B receives message via WebSocket, User B reacts to message, User A receives reaction event, User A creates DM with User B, both users exchange DM messages, User A creates group DM with Users B and C, all users receive messages, User A adds User D to group DM, User D receives history and new messages, User B mutes group DM, User B doesn't receive notifications, User A mentions User B in channel, User B receives notification, User A uploads large attachment (20MB), verify thumbnail generated, User A edits message with mentions, mentions re-parsed. Use real database, Redis, MinIO, and WebSocket connections. Simulate realistic user behavior with delays. ACCEPTANCE: All flows complete successfully, end-to-end latency acceptable, no errors, realistic user experience. VERIFY: Run e2e tests, measure total flow time, verify all events received in correct order.
-[ ] NAME:9.6. Update API Documentation DESCRIPTION:Update Swagger/OpenAPI documentation for all new endpoints. Document: Reaction endpoints (PUT/DELETE/GET /api/v1/messages/:messageId/reactions/*), Attachment endpoints (POST /api/v1/messages/:messageId/attachments), DM endpoints (POST/GET/PATCH/DELETE /api/v1/users/@me/channels, /api/v1/channels/:channelId/*), DM message endpoints (POST/GET/PATCH/DELETE /api/v1/channels/:channelId/messages/*), Notification settings endpoints (PUT/GET /api/v1/channels/:channelId/notification-settings). For each endpoint document: path parameters, query parameters, request body schema, response schema, error responses (400, 401, 403, 404, 429, 500), authentication requirements, rate limits, examples. Add schemas for: Reaction, Embed, Attachment, DMChannel, DMParticipant, NotificationSettings. Verify Swagger UI renders correctly. ACCEPTANCE: All new endpoints documented, schemas complete, examples provided, Swagger UI renders without errors. VERIFY: Open Swagger UI, test each endpoint from UI, verify request/response match documentation.
-[ ] NAME:9.7. Update Database Documentation DESCRIPTION:Update packages/api/DATABASE.md to document all new tables. Add sections for: reactions table (schema, indexes, constraints, relationships), message_embeds table, message_attachments table, custom_emojis table, dm_channels table, dm_channel_participants table, dm_notification_settings table. For each table document: purpose, columns (name, type, constraints, description), indexes (name, columns, type, purpose), foreign keys (column, references, on delete/update), check constraints, unique constraints, example queries (insert, select, update, delete), performance considerations, migration file reference. Add ER diagram showing relationships between new tables and existing tables. Update table of contents. ACCEPTANCE: All new tables documented, ER diagram updated, documentation clear and complete, no outdated information. VERIFY: Review documentation for completeness, verify all constraints documented, test example queries.
-[ ] NAME:9.8. Create Cleanup and Maintenance Scripts DESCRIPTION:Create packages/api/src/scripts/cleanup-orphaned-attachments.ts to find and delete attachments in MinIO without database records (query message_attachments, list MinIO objects, delete orphans), log deleted files. Create packages/api/src/scripts/cleanup-old-reactions.ts to optionally clean up reactions older than X days (configurable). Create packages/api/src/scripts/vacuum-dm-channels.ts to clean up DM channels with no active participants (all participants left). Create packages/api/src/scripts/regenerate-thumbnails.ts to regenerate missing thumbnails for existing attachments. Add CLI interface using commander or yargs. Add dry-run mode for all scripts. Add logging with Pino. Add error handling and transaction support. Document scripts in README. ACCEPTANCE: All scripts work correctly, dry-run mode prevents accidental deletions, logging provides audit trail, scripts can be run via npm scripts. VERIFY: Run each script in dry-run mode, verify output, run with real data (test database), verify cleanup successful.
-[ ] NAME:9.9. Performance Optimization and Profiling DESCRIPTION:Profile application under load using clinic.js or 0x. Identify bottlenecks in: message creation with embeds/attachments, reaction operations, DM routing, presence broadcasting. Optimize: add missing database indexes (analyze EXPLAIN output for slow queries), optimize N+1 queries (use joins or batch loading), add Redis caching for frequently accessed data (DM participants, custom emojis, user presence), optimize WebSocket broadcasting (batch events, use Redis pub/sub efficiently), optimize attachment upload (stream directly to MinIO, parallel thumbnail generation), optimize markdown parsing (cache parsed content). Measure before/after metrics: query time, endpoint latency, memory usage, CPU usage. Document optimizations in PERFORMANCE.md. ACCEPTANCE: p95 latency improved by 30%, database query time reduced, memory usage stable under load, no performance regressions. VERIFY: Run load tests before and after, compare metrics, verify no regressions in functionality.
-[ ] NAME:9.10. Final Integration Verification DESCRIPTION:Perform comprehensive verification of entire implementation. Run all tests: unit tests (npm test), integration tests, load tests, e2e tests. Verify test coverage ≥80% for all new code. Run linter (npm run lint) and fix all issues. Run type checker (npm run type-check) and fix all errors. Run build (npm run build) and verify no errors. Test all API endpoints manually using Swagger UI or Postman. Test all WebSocket events using WebSocket client. Verify database migrations run cleanly (up and down). Verify MinIO integration works. Verify Redis caching works. Test error scenarios: network failures, database failures, MinIO failures, invalid input, unauthorized access. Review code for security vulnerabilities: SQL injection, XSS, CSRF, authentication bypass, authorization bypass. Review code for performance issues: N+1 queries, missing indexes, memory leaks. Create final verification checklist and mark all items complete. ACCEPTANCE: All tests pass, coverage ≥80%, no linter errors, no type errors, build succeeds, all manual tests pass, no security vulnerabilities, no performance issues. VERIFY: Run full test suite, run linter, run type checker, run build, manual testing, security review, performance review.